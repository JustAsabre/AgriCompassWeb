# Environment Variables

# Session Secret (Change in production!)
SESSION_SECRET=agricompass-dev-secret-change-in-production

# Server Port
PORT=5000

# Database URL (Optional - uses in-memory storage if not provided)
# DATABASE_URL=postgresql://user:password@localhost:5432/agricompass

# Node Environment (development/production)
# NODE_ENV=development

# Email Configuration

# Primary Email Service: Resend (FREE - 3,000 emails/month, 100 emails/day)
# Sign up at https://resend.com and get your API key
# This is the recommended option for development and small-scale production
RESEND_API_KEY=re_123456789
EMAIL_FROM=AgriCompass <onboarding@resend.dev>

# Optional: SMTP Fallback (for production scaling)
# Automatically used if Resend fails or is not configured
# For Gmail: Use app password (https://support.google.com/accounts/answer/185833)
# For production: Use SendGrid, AWS SES, or similar
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM="AgriCompass" <noreply@agricompass.com>

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5000
